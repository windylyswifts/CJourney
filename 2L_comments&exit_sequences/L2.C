#include <stdio.h>

int main(){
    //double "/" means that this 
    /*What this means is that you can put mutiple lines and turn it into a comment. Very similar to HTML and CSS comment
    
    Yeah
    */
   /*
    escape sequences = characters combination consisting of a backslash \
    followed by a letter or combination of digits. They specify actions within 
    a line or string in the text. 
    Ex=
    \n = newline
    \t = tab
    \" = adds a quote
   */
    printf ("wassup\ndawg\n");
    printf ("I\tdont\tlike\tto\tlearn\twithout\tearphones.");
    return 0;
}